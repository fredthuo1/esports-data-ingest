const websocketClient = require('./services/websocketClient');

// The WebSocket client is initialized and started when we require it.
// If you plan to add more functionalities or services, you can do so here.
