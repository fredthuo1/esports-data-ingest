module.exports = {
    WEBSOCKET_URL: 'ws://localhost:8080/2579048',
    RECONNECTION_DELAY: 5000,  // Time (in milliseconds) to wait before attempting to reconnect
    // Add any other configuration constants as needed
};
